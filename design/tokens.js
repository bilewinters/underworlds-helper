const pallet = {
  black: '#000000',
  blue: '#87CEEB',
  gold: '#FFD700',
  green: '#008000',
  grey: '#808080',
  white: '#FFFFFF',
};

const color = {
  primary: pallet.green,
};

const button = {
  color: {
    primary: pallet.white,
    text: pallet.black,
  },
};

const text = {
  primaryColor: pallet.white,
  font: 'sava-pro-semibold',
  headingSize: 56,
  titleSize: 24,
  labelSize: 16,
};

export default {
  pallet,
  color,
  text,
  button,
};
